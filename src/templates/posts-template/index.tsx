import { DefaultLayout } from '$components/layout/default-layout';
import { InConstruction } from '$components/ui/in-construction';

export function PostsTemplate() {
  return (
    <DefaultLayout>
      <InConstruction />
    </DefaultLayout>
  );
}
